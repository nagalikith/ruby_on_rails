- Volunteer.all.each do |volunteer|
  %tr
    %td= volunteer.name
    %td= volunteer.contactnumber
    %td= volunteer.youngPerson
    %td= volunteer.email
    %td= volunteer.hours
    %td= volunteer.target
    %td= link_to 'Edit', edit_volunteer_path(volunteer), class: "ajax-modal"
    %td= link_to 'Delete', volunteer, method: :delete, data: { confirm: 'Are you sure?' }
    