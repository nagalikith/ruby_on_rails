%h1 Stories

%ul.nav.nav-tabs
  %li.nav-item
    = link_to "Case Studies", case_studies_path, class: "nav-link"
  %li.nav-item
    = link_to "Event Feedback", event_feedbacks_path, class: "nav-link active"

%br

%h3 Event Feedback

- if @event_feedbacks.length > 0

  %p There are #{@event_feedbacks.size} pieces of feedback to search through.

  %table
    %thead
      %tr
        %th Date
        %th Event Name
        %th Comment

    %tbody#feedbackBody
      = render "feedback_table"
- else
  .error-message
    %h5 No feedback exist

%br

= link_to 'New Event feedback', new_event_feedback_path, class: "btn btn-secondary ajax-modal"
